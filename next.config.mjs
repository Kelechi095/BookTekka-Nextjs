/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["img.freepik.com", "ng.jumia.is", "lh3.googleusercontent.com", "res.cloudinary.com"],
    }
};

export default nextConfig;
