/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["img.freepik.com", "ng.jumia.is", "lh3.googleusercontent.com", "res.cloudinary.com", "media.istockphoto.com", "books.google.com" ],
    }
};

export default nextConfig;
